{
  "name": "codemap",
  "version": "0.1.0",
  "description": "Local-first interactive visual map of a software project: file tree + flowchart + dependency graph.",
  "keywords": ["code-map", "dependency-graph", "flowchart", "architecture", "developer-tools"],
  "type": "commonjs",
  "bin": {
    "codemap": "bin/codemap.js"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node bin/codemap.js",
    "test": "node test/run.js",
    "check": "node --check src/cli.js && node --check src/index.js && node --check web/app.js && node test/run.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "files": [
    "assets",
    "bin",
    "src",
    "web"
  ]
}
